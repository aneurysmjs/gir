{
  "name": "@gir/dashboard",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "commit": "git-cz",
    "start": "vite",
    "build": "vite build",
    "lint": "eslint './src/**/*.{ts,tsx}'",
    "stylelint-check": "stylelint-config-prettier-check",
    "test": "pnpm -w t -- -c=\"${INIT_CWD}/packages/dashboard/jest.config.ts\""
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-conventional-changelog"
    }
  }
}
